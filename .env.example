# SLMG Beverages BI Portal — Environment Variables
# Copy this file to .env and fill in values

# ── Security ──────────────────────────────────────────────────
SECRET_KEY=your-super-secret-flask-key-change-this
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this
JWT_EXPIRES_HOURS=8

# ── Database ──────────────────────────────────────────────────
# SQLite (default — no setup needed):
DATABASE_URL=sqlite:///slmg_portal.db

# PostgreSQL (for production on Render/Railway/Heroku):
# DATABASE_URL=postgresql://user:password@host:5432/slmg_bi_portal

# ── AI (Optional — Anthropic Claude) ──────────────────────────
# Without this key, the portal uses the built-in rule-based analytics engine.
# Add your Anthropic API key to unlock Claude-powered AI executive summaries.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ── Cookie Security ───────────────────────────────────────────
JWT_COOKIE_SECURE=false  # Set to: true in production (requires HTTPS)

# ── CORS ─────────────────────────────────────────────────────
CORS_ORIGINS=*

# ── Debug ────────────────────────────────────────────────────
DEBUG=false
